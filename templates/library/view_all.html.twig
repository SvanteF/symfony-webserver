{% extends 'base.html.twig' %}

{% block title %}Alla böcker{% endblock %}

{% block body %}
{#
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
#}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">


<div class="wrapper">

    {% include 'library/navbar.html.twig' %}

    <p>Klicka på titeln för att enbart se detaljer om boken, <i class="fas fa-trash"></i> för att ta bort den och <i class="fa-solid fa-pen"></i> för att editera.</p>

    <ul class="no-bullets">
        {% for book in library %}
            <li>
                <a href="view/{{ book.getId|e }}">{{ book.getTitle|e }} </a>{#({{ book.getIsbn|e }}) {{ book.getAuthor|e }}#} 
                <form action="{{ path('library_delete_by_id') }}" method="post" style="display:inline;">
                    <input type="hidden" name="id" value="{{ book.getId }}">
                    <button type="submit" style="background:none; border:none;">
                        <i class="fas fa-trash"></i>
                    </button>
                </form>
                <a href="{{ path('book_update_get', {'id': book.getId}) }}">
                    <i class="fa-solid fa-pen"></i>
                </a>
            </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}




